# Server
PORT=4000
NODE_ENV=development

# Database
DATABASE_URL=postgresql://urlytics:urlytics_password@localhost:5432/urlytics_db
REDIS_URL=redis://localhost:6379

# Auth
# Production startup rejects defaults and secrets shorter than 32 characters.
# Access cookies last 15 minutes; rotating refresh sessions last 30 days.
JWT_SECRET=change_me_to_at_least_32_high_entropy_characters

# CORS — comma-separated allowed frontend origins
CORS_ORIGINS=http://localhost:3000

# Public base URL for short links (no trailing slash)
SHORT_URL_BASE=http://localhost:4000/api/r

# Billing is retained for future use but disabled during the free first phase
BILLING_ENABLED=false

# Production also requires DATABASE_URL, REDIS_URL, SHORT_URL_BASE, and at least
# one HTTPS-only CORS_ORIGINS entry. Registration provisions an OWNER workspace.
